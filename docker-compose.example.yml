# Drop-in replacement for the legacy container: mount a directory containing
# config.json at /config.
services:
  varde:
    image: bendiksolheim/varde:latest
    restart: unless-stopped
    volumes:
      - ./config:/config
    ports:
      - 3000:3000
